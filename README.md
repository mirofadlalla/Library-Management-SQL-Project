# Library-Management-SQL-Project
This project implements a Library Management System using SQL. It includes DDL, DML, queries, stored procedures, and reporting operations for managing books, members, employees, branches, and transactions (issued and returned books).
